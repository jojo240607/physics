[package]
name = "phy-rigid"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
readme.workspace = true
description = "刚体动力学子系统:顺序冲量求解、CCD、休眠、传感器、碰撞层、岛屿并行与场景 DSL。"

[dependencies]
phy-core = { path = "../phy-core" }
phy-math = { path = "../phy-math" }
phy-field = { path = "../phy-field" }
nalgebra.workspace = true
serde.workspace = true
serde_json = "1.0"
num-traits = "0.2"
rayon = "1.10"

[features]
# C3 (commercial-readiness plan §11): per-stage step() timing.
# Off by default so the hot path stays zero-cost (no Instant::now calls).
profiler = []
